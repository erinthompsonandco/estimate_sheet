<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.thompson.erin.hvacforms1221">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:icon="@mipmap/ic_launcher"
            android:label="Jobs"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".FormOptions"
            android:icon="@mipmap/ic_launcher"
            android:label="Options" />

        <activity
            android:name=".FormView"
            android:icon="@mipmap/ic_launcher"
            android:label="View Old Jobs" />
        <activity
            android:name=".splitSend"
            android:icon="@mipmap/ic_launcher"
            android:label="Split System info" />
        <activity
            android:name=".packageUnitSend"
            android:icon="@mipmap/ic_launcher"
            android:label="Package Unit info" />
        <activity
            android:name=".viewPackageSystem"
            android:label="Package Unit info" />
        <activity
            android:name=".viewSplitSystem"
            android:label="Split Unit info" />
        <activity
            android:name=".package_selecter"
            android:icon="@mipmap/ic_launcher"
            android:label="System Types" />
    </application>

</manifest>
